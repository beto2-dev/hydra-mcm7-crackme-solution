# password candidates for the verify workflow (one per line)
# Lines starting with # are comments.
#
# The previous candidates (HydraKey6d417277, HydraKeyb77d9659) were hits of
# the pre-correction 16-byte model and are rejected by the real binary
# (the console reader NUL-pads the token to a 64-byte string). They are
# retired. New hits from the corrected 64-byte model search will be
# submitted here after the gold (Unicorn) validation.
#
